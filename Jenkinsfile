pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo ("Testing now")
                sh'''
                  docker version
                '''
            }
        }
        
    }
}
